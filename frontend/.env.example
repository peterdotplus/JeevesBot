# JeevesBot Frontend Environment Variables
# Copy this file to .env.local and modify the values as needed

# Backend API URL
BACKEND_URL=http://localhost:3001

# Public application name (displayed in browser tab and PWA)
NEXT_PUBLIC_APP_NAME=JeevesBot Calendar

# Backend authentication credentials
# These should match the credentials in backend/config.json
BACKEND_USERNAME=admin
BACKEND_PASSWORD=password123

# Next.js configuration
NODE_ENV=development

# Optional: Analytics and monitoring
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
# NEXT_PUBLIC_SENTRY_DSN=

# Optional: Feature flags
# NEXT_PUBLIC_ENABLE_DARK_MODE=false
# NEXT_PUBLIC_ENABLE_OFFLINE_MODE=false

# Development settings
# PORT=3000

# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Other variables are only available on the server
# - Never commit .env.local to version control
# - For production, set these variables in your hosting platform
